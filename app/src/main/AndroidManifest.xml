<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/aizuomaoche"
        android:label="@string/app_name"
        android:roundIcon="@drawable/aizuomaoche"
        android:supportsRtl="true"
        android:theme="@style/Theme.XIZHOU">

        <activity
            android:name=".activity.XCreateActivity"
            android:exported="false" />
        <activity
            android:name=".activity.FacebookCreateActivity"
            android:exported="false" />
        <activity
            android:name=".activity.CalenderCreateActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.YoutubeCreateActivity"
            android:exported="false" />
        <activity
            android:name=".activity.HistoryDisplayActivity"
            android:exported="false" />
        <activity
            android:name=".activity.ScanResultActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
